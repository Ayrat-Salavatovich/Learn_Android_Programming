<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/buttonAdjustResize"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="adjust"
        android:text="@string/adjust_resize" />

    <Button
        android:id="@+id/buttonAdjustPan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buttonAdjustResize"
        android:layout_centerHorizontal="true"
        android:onClick="adjust"
        android:text="@string/adjust_pan" />

    <Button
        android:id="@+id/buttonAdjustUnspecified"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buttonAdjustPan"
        android:layout_centerHorizontal="true"
        android:onClick="adjust"
        android:text="@string/adjust_unspecified" />
    
    <Button
        android:id="@+id/buttonAdjustNothing"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buttonAdjustUnspecified"
        android:layout_centerHorizontal="true"
        android:onClick="adjust"
        android:text="@string/adjust_nothing" />

</RelativeLayout>